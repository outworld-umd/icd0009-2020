@using Resources.Views.Home.Privacy
@{
    ViewData["Title"] = Privacy.PrivacyPolicy;
}
<h1>@ViewData["Title"]</h1>

@foreach (var claim in User.Claims) {
    <p>@claim.Type - @claim.Value - @claim.ValueType</p>
}